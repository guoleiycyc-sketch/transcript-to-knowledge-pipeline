# Changelog — transcript-to-knowledge-pipeline

维护纪律见 `MAINTENANCE.md`：改 scripts/references 必升版本号 + 在此追加条目 + 跑 `scripts/regression_check.py`。对外发布走 `make_release.py`（脱敏构建，勿手推公开仓）。

## 2.4.3（2026-09-07）
- **WorkBuddy 上架版构建支持**（make_release 四项增强）：①执行命令路径双宿主化——SKILL.md 与 references 里的本机绝对路径在公开版替换为 `<skill目录>` 占位（INSTALL.md/README 讲安装路径本身，豁免）；②本机模型槽位别名在公开版泛化为「轻量模型」（含模型档位复合表述与内容审查拦截两句的先行句级改写）；③项目侧审计脚本引用泛化为「场后审计（自备脚本）」；④公开版 SKILL.md frontmatter 注入 `description_zh`/`description_en`（WorkBuddy 市场短显示描述，私源不携带避免双处维护）。另：公开版句式净化（命令式重词降密度、词距收拢）
- README_TEMPLATE 更新至 v2.4 特性口径（+单场委派模式、+37 条坑库、WorkBuddy 安装说明、`<skill目录>` 释义）
- 6 个公开脚本 docstring 补英文安全声明（stdlib-only / 无网络 / 无子进程 / 无动态执行 / 读写边界）——安全审查友好

## 2.4.2（2026-09-07）
- **description 重写为意图导向**（触发短语显式化 + 边界排除句）：新增「仅快速回顾一场录音 / 检索往期观点原话（→kb）/ 已有 01 清洗稿改纪要（→meeting-minutes）/ 出评估分析报告（→ting-jiang 等）的不用本 skill」排除清单，治与 kb/meeting-minutes 的边界竞争；触发短语补齐「清洗」「老规矩/标准管线出全套」「复现上次那条管线」「委派模式」等真实说法。来源=skill-creator 触发盲测 20 条真实查询的 improver 产出（本轮人工补「清洗」一词并审核全部边界句）；量化复测因裸 -p 环境地板效应（正负例全零触发）无法区分优劣（数据与诊断：~/.claude/skill-evals/transcript-to-knowledge-pipeline/结论_2026-09-07.md），应用依据为定性边界价值，项目内触发仍由<项目根> CLAUDE.md 硬性路由保障。

## 2.4.1（2026-09-07）
- 原蓝本 `recording-knowledge-pack` 从 `~/.claude/skills/` 卸载封存至 `~/.claude/skills-disabled/`——其描述与本 skill 高度重叠（触发竞争），且内部仍产 `index.html`（违反 v2.3.7 命名规约）、不含 2.2.0 后全部修复；渲染器/模板早已内联本 skill，无功能损失。SKILL.md 关系表同步标注退役。

## 2.4.0（2026-09-07）

- **新增单场委派模式**（references/10-delegation-protocol.md）：新场次 01 由轻量模型子代理生产，主对话只做映射裁决+质检+模块提炼——Task 7 两场试点实测主对话清洗成本降约 70%、忠实度 0.71-0.74 正常区间、audit 7/7。启用条件=项目 pipeline.config.json 的 delegation.enabled；与 references/09 批量模式互补。
- 新坑 P33-P37 入库：轻量模型版 01 骨架致 verify_quotes 跳过（用引文终校替代脚本）/引文禁凭记忆写/口吃保真/误带金句拒录/映射初判须跨场硬证据复核。
- SKILL.md：流程图加委派分支、references 表加 10 行。

## 2.3.9（2026-09-07 · extract_atoms 同日撞号残留修复：冲突告警 + 人物小节头误抽）
- **extract_atoms 同 id 内容冲突从静默吞没改为响亮告警**（P32）：按 id 去重时若同 id 的存量内容与新抽取不一致（同日多场 sid 撞号，或源文件修订后金句编号错位），打印冲突清单而非无声丢弃——2.3.5 的 date_suffix_map 只按当日排序分配后缀，与历史入库顺序不一致的 4 个排序在前场次（08-20 AI合作方A/08-21 企业AI导入/08-22 播客收听/09-02 自我介绍）的方法论+洞察+引语仍被前一场存量吞没，「全量补回」当时并未兑现；本次配套清理 130 条幽灵/重复原子后重抽回填（atoms 8519→8478，含删 130 增 89），4 场全部修复
- **05 人物卡小节头不再误抽为人物**：`以下为被提及者（不在场）`、`其他在场/被提及（简条）`、裸 `被提及者`/`被提及人物` 等小节标题此前按 `## 标题` 正则入为 person 原子（过滤条件 `'（被提及'` 挡不住这些写法），过滤规则扩展至全部「被提及*」及「其他在场」前缀标题；v2.3.8 新格式的被提及人物本体不受影响、照常入库；库内 4+2 条 bogus 人物头清零（2 条为审查返工补删）
- **regression_check 样板 B 改为自构造临时场次**：原「无 00 老场」样板引用真实客户丁 08-19 场——该场被补 00 后断言前提永久失效（回归报 3 项假失败），且引用真实场目录属 skill 对项目的耦合、外发安装不成立；现于 tempfile 内构造最小场次（01 两回合 + 02 一段、无 00）渲染后即弃，断言逻辑与样板 A 未动，回归全绿
- 回归全绿；残留 15 处冲突告警属「源文件修订后引语重编号」漂移（08-12×11、09-03×4，无丢失），留待源文件稳定后处理

## 2.3.8（2026-09-06 · 三层阅读 v2：①摘要卡+②分段速览）
- **① 执行摘要升级为醒目可折叠摘要卡**（details 默认展开）：00 的全 BLUF 四节（局势/本场决策/待办表精简/风险行）收进一张卡，卡底「跳过摘要，看全库内容 ↓」锚到③层首模块——写作侧零改动
- **② 总结层改为「分段速览」行卡**：每段一行（段号+标题+摘要句+「展开该段」），原料从段结论首句换成各段 `## 摘要`（回退：段结论→副标题）；待办/风险表从②层移入①卡（每信息只出现在一层）
- **层间锚点**：①卡内人名→#people、待办 Owner 列整格→#people（覆盖「C/A」「部落」等昵称写法）、卡N→#insight-N（仅本场确有卡号）；圈号①-⑳原生链接不变
- 阅读协议横幅文案换为「按需下潜：读完摘要即可走，有意思再往下点——省的是你的大脑」
- ②层字数护栏改口径：行=段号+标题+摘要句(≤100)，限额 max(1200, 段数×130)，控件/卡胶囊不计入
- **修复 regression_check.py 自 v2.3.7 起必崩**：读死 `index.html` 路径→改为 glob 渲染产物（v2.3.7 改输出名时未同步此脚本，改渲染器后的例行回归首跑即 FileNotFoundError）；新增 v2.3.8 断言（①摘要卡+跳过锚、②速览行=段数、老场降级无三层）
- 向后兼容验证：无 00 的老场渲染与 2.3.7 逐字节一致（除新增惰性 CSS，正文 0 差异）
- 文档同步：SKILL.md 结构图/三层说明、references/03 §9 规格表、references/04 渲染速览、pipeline_check 检查 8 文案

## v2.3.7（2026-09-06）
- HTML 产物文件名改为 `<场次目录名>.html`（用户要求：index.html 无辨识度）；render_pack.py 输出名变更，pipeline_check.sh 检查 5 改动态检测（兼容旧场 index.html）；SKILL.md/references 说明同步。存量场次维持原样。

## 2.3.6（2026-09-04 · 发布审核补漏）
- 删除 SKILL.md 创建期遗留的「与已有三场知识包的关系」表（场次追踪由项目主题 README 承担，skill 内保持零内部叙事；表内场次名/日期属未脱敏叙事）
- make_release 脱敏增强：场次编号通用正则（第 N 场→某场次）、「某英文场次」等词条、P1 案例组织细节泛化、内部规模数字泛化——v2.3.4/2.3.5 公开仓存在的 6 处场次叙事残留随本版清除

## 2.3.5（2026-09-04 · 实战反哺三修）
- **extract_atoms 同日多场 sid 撞号修复**（P30）：id 只含日期→同日场次互吞；改 `date_suffix_map`（第一场无后缀兼容存量、第二场起 b/c/d）。实战暴露**历史欠账**：多个同日场次数百条原子自 8 月建库起从未入库，修复重跑后全量补回，方法论/引语视图已重渲
- pipeline_check 反方计数正则放宽（`^\*\*反方` 前缀匹配）——v2.1 反方分级标注 `**反方（A）**` 形态与旧检查 `^\*\*反方\*\*` 不匹配，曾报「反方 0」假警
- pipeline_check 检查 7 全局层路径回退（`_成果目录/` 等子目录场次向上多找一层 `_全局资产`）

## 2.3.4（2026-09-04 · 可发布化）
- build_html.py 首场死数据清理（-371 行）：SEG_META/PEOPLE_COLOR 等 6 组硬编码与 6 个首场函数 stub 化、main 删除——一次性消掉 50 处人名+1 价格，render_pack monkey-patch 契约不变（回归全绿）
- render_method 母版标题改「方法论清单」
- 新增 `scripts/make_release.py` 发布管线：本地=私有源 → 词汇映射（人名→代号/公司→泛称/路径→占位）+ 句子级改写（变体归正句）+ 对外 README 模板 + 敏感扫描断言（0 命中才放行，含独立兜底词表——首版曾漏「A」，教训：扫描词表不能只从映射表生成）
- 首次执行全套脱敏发布：公开仓 v2.0.0（含 289 处敏感命中）→ 干净 v2.3.4

## 2.3.3（2026-09-04 · 打样迭代三）
- 方法论清单「命中校验/对照」类小节默认折叠（标注「质检数据」）
- ASCII 结构图图形化：`try_render_graph` 定向解析两种范式——分层带（层名行+圈号条目，读作者标的「—— 绿/蓝/橙」颜色意图）与线路流（→≥2）；解析失败回退深色终端卡（深底亮字+「◈ 结构图」标签）
- 全文增强：正文「 → 」渲染为彩色箭头（87 处/样板）；圈号①-⑳ 全文变可点段链接（75 处/样板）
- 图形节点（⑬/卡N）可点击下潜到对应段/洞察卡

## 2.3.2（2026-09-04 · 打样迭代二）
- 主题分段（seg×N）移至页面最末（全文层垫底），侧栏同步；模块顺序=brief→digest→people→map→insights→method→strategy→glossary→data→seg
- 人物只留一处：删②层「本场人物」索引行；人物卡 grid 删 tagline（与折叠详情首句重复）；保留一处两层（卡片速览+详情折叠）
- 折叠原则确立「结论展开、过程折叠」：16 段「②详析」默认折叠（摘要+金句保持展开）
- 配套 JS：锚点跳入折叠区自动展开 details（折叠不吞跳转）
- **修复回归**：`.raw-body .ts` 灰字规则覆盖金句卡 `.quote .ts` 白字（蓝底灰字，用户实测发现）→ 行式样式收窄到 `.line .ts`（P29）

## 2.3.1（2026-09-04 · 打样迭代一）
- overview 收编：有②层时不再渲染自动总览（母题/人物索引并入②层、分段目录被②层超集取代、导航卡被协议横幅取代）；定制总览（00_总览.md）与无②层老场不受影响
- 修复 method 标题 bug（目录名硬拼→固定「方法论清单」）
- 修复 extract_people 把说明节「提及未出场（一句话索引）」误当人名

## 2.3.0（2026-09-04 · 三层阅读结构，B 规格）
- ① 简报层：00_执行摘要的局势+决策节渲染进 HTML 首屏（此前完全不进网页）；待办/风险自动拆给②层折叠——写作侧零改动
- ② 总结层：render_digest 机器编译（段结论首句+洞察卡胶囊+待办折叠表，全带锚点）——禁手写总结层（收敛不是删是索引）
- ③ 完整层不变；页面顶部阅读协议横幅；侧栏①②导航
- inline_fmt 支持 `[文本](#锚点)` 下潜链接
- 护栏：渲染自检查锚点有效性+②层字数（限额=max(1200, 段数×95)）；pipeline_check 检查 8（00 存在+段结论覆盖）
- 文档：references/03 §9 三层规范（可预期性×阅读成本）；07 模板 00 写法更新

## 2.2.0（2026-09-04 上午 · 42 场实战经验回流批）
- **渲染器 6 类缺陷修复**（08-19 已知、15+ 场复现）：seg-subtitle 句边界截断 clip_sub（旧 [:50] 腰斩）+孤儿 `**` 清理；映射注跨段去重 dedupe_asides；`:is`→`.line` 作用域修复原文节选徽章失效；死 CSS ~90 行删除+移动端 chip 误伤解除；对比色全量换深一档（#3370ff→#2b62d6、#00b894→#0c7f63、#e8910a→#9a5f00）；人物口径「X 位发言者+Y 位被提及」
- 渲染质量自检护栏（副标题星号/旧色/死类/重复注 4 查，render_pack 末尾自动）
- extract_atoms.py 递归扫描（嵌套场次不再漏抽，第 33/42 场两次踩坑根治；双日期目录名支持；真实重跑补 66 条漏抽原子）
- pitfalls 库修复与收编：P13/P14 重号→P22/P23；新增 P24-P28（正文加粗炸 verify/渲染契约硬依赖/重写 01 重核+续段/分段读防脑补/子代理输出纪律）
- SKILL.md 速览表 P10→P28 全量
- 档位正式化六档（极轻/轻/轻加强/专项/标准/重装）+目录落位判据+atoms 入库线（references/03）
- 说话人标签 5 类结构异常进 references/01（多人压标签按时段/双身份/视频原声误归/会后混录/旁听无标签）
- 清洗原则 4→6 条+边缘类型规范（英文场/屏幕操作段/特长场）（references/02）
- 新建 references/09-batch-cleansing.md（轻量模型子代理流水线：输出纪律模板/质检分工/运营事件/批量收尾）
- pipeline_check 收尾三问（经验回流强制化：新坑入 06/渲染自检 ✓/增量同步）

## 2.0.0（2026-08-19 · 三方评审版）
- 00_执行摘要、03/08 互斥判据、金句引用制、全局资产层、atoms+视图（详见 references/03 v2 节）
