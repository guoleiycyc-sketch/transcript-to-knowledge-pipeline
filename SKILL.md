---
name: transcript-to-knowledge-pipeline
description: "把一份对话类录音转写稿（飞书妙记/得到大脑/Tencent Meeting/Whisper/手工整理稿），经过严格的说话人映射和清洗，产出 9 模块结构化知识包 + 自包含可搜索深浅主题 HTML 的完整工程化管线。本 skill 是**自包含**的工程化管线（已内联 HTML 渲染器与格式模板，无外部 skill 依赖）——把管线执行过程中的全部陷阱（说话人误判、ASR 错字、格式 bug、字段遗漏、HTML 渲染失败）显式化为 6 次发布前必查和持续累积的 pitfalls 库。一旦用户要把转写稿整理成知识包/网页文档/可搜索页面，并希望过程严格、有检查清单、可复用，立刻触发本 skill。"
version: 1.0.0
---

# Transcript → Knowledge Pipeline

把一份对话类录音转写稿，严格、零幻觉地整理成结构化知识包 + 可分享 HTML 的工程化管线。

## 与现有 skill 的关系

| Skill | 关系 |
|---|---|
| `recording-knowledge-pack`（原蓝本） | **已内联**：其 HTML 渲染器（`scripts/build_html.py` + `render_pack.py`）与格式模板（`references/format-templates.md`、`build-html-guide.md`）已复制进本 skill，**单 skill 独立运行，无需额外安装**。 |
| `echo-weave` | **互补**：echo-weave 按场景选模式（纪要/访谈/故事/…）；本 skill 是固定管线的工程执行。echo-weave 的洞察卡片方法被本 skill 第 3 阶段引用。 |
| `huawei-nas` / `lark-minutes` / `agent-reach` | **上游源**：从 NAS / 飞书妙记 / 互联网抓取转写稿后，再走本 skill。 |

## 触发条件

用户说且不仅限于：
- "把这份录音/会议/访谈/转写稿整理成知识包/网页文档/可搜索页面"
- "清洗这个对话稿 / 做会议纪要 HTML"
- "做整套知识沉淀、9 模块、双击即开网页"
- "复现上次那条管线 / 按标准管线出"

## 全流程（5 阶段 + 6 检查）

```
① Intake + 说话人映射  ←—— 最高风险阶段
      ↓
② 清洗（01_清洗稿）    ←—— 必经两次：标签脏数据检测 + 修复
      ↓
③ 9 模块产出（02–09）   ←—— 金句格式 `**【姓名】** [ts] 原话` 必准
      ↓
④ HTML 组装（render_pack.py）
      ↓
⑤ 跨场呼应 + README
      ↓
╔══════════════════════════════════╗
║ 发布前 6 次必查（见 references/05） ║
╚══════════════════════════════════╝
```

每阶段开工前必读对应 references：

| 阶段 | 参考 |
|---|---|
| ① Intake + 说话人映射 | `references/01-intake-speaker-mapping.md` |
| ② 清洗 + ASR | `references/02-cleansing-asr.md` |
| ③ 9 模块 | `references/03-nine-modules.md` |
| ④ HTML | `references/04-html-assembly.md` |
| ⑤–⑥ 检查 + pitfalls | `references/05-quality-checks.md`、`references/06-pitfalls.md` |
| 模板 | `references/07-templates.md` |

## 全局硬规范

### 金句/证据格式（最容易错）

所有金句、原文节选、洞察证据、人物代表发言一律：

```
**【姓名】** [MM:SS] 或 [HH:MM:SS] 原话
```

**错一个 `】`，HTML 渲染器就识别不了 → 整篇金句丢失交互。**

每阶段交付前必须检测（详见 `references/05`）：

```bash
grep -cE '^\*\*【[^】]*\*\* \[' <场次目录>/01_清洗稿.md   # 应 = 0
```

### 说话人映射（最高风险）

**绝不能凭某人在原文中被称呼的方式判定其 ASR 标签**。本次（2026-08-09 场）的反面教训：

> 说话人3 被郭磊 / Harry 当面叫"夏老师" → 我两次判"说话人3 = 夏老师"
> 实际：说话人3 = 郭志梁（主导商业规划、做视频号、润商会合伙人、公司年营收一亿）
> 夏老师是另一人，发言不多，未被 ASR 单独识别

详细方法论见 `references/01`——**角色指纹 + 时间线核对 + 用户二次确认**。

### 产物放独立子目录

```
<项目根>/
  <场次名>_YYYY-MM-DD/
    ├── 01_清洗稿.md
    ├── 02_主题整理/
    ├── 03_方法论清单.md
    ├── 04_术语表.md
    ├── 05_人物角色卡.md          ← 必须 ## 人名 + **背景与立场**：+ 约 XX%
    ├── 06_关键数据速查.md
    ├── 07_议题关联地图.md         ← **母题**：+ ```ASCII 图（必须代码块）
    ├── 08_洞察卡片.md             ← 每张 ## 卡 N · 标题 + 核心/证据/启示/反方
    ├── 09_战略诊断与行动清单.md   ← 仅咨询/诊断类
    ├── README.md
    └── index.html                 ← render_pack.py 一键生成
```

### 忠实原则

- 金句原话一字不改。
- ASR 能确定的订正写进对照表；不确定的标 `[ASR/存疑]`，**不臆造**。
- "诚实标注 > 完美无瑕"。

## 关键陷阱速览（详细见 `references/06-pitfalls.md`）

| # | 陷阱 | 后果 | 检测/修复命令 |
|---|---|---|---|
| P1 | 凭称呼判定说话人 | 整篇说话人标错 | 见 01 的"角色指纹"法 |
| P2 | 漏写右 `】`：`**【人名** [` | 金句格式识别失败 → HTML 无金句卡 | `grep -cE '^\*\*【[^】]*\*\* \['` 应=0；用 `sed -i '' -E 's/^\*\*【(人名1\|人名2\|…)\*\* /**【\1】** /'` 一把修 |
| P3 | 金句速查、原文节选、证据**字段头写错** | 渲染器无法提取 | 模板见 `references/07` |
| P4 | 议题地图 / 洞察地图不用 ``` 代码块 | 渲染为普通段落 | 强制检查 |
| P5 | 给圆桌/闲聊硬加 09 战略诊断 | 显得别扭 | 类型判定见 `references/01` |
| P6 | 洞察卡片不写反方 | 变成断言 | 反方/边界/张力必填 |
| P7 | 大段清洗稿用 `Write` 一次写导致 JSON 超长截断 | 文件半截、内容丢失 | **必须分块**：Write 头部+前几节 → Edit 用唯一锚点（如 `<!-- APPEND_MARKER_N -->`）逐块追加 → 末尾去锚 |
| P8 | 没立刻用 `grep` 自检回合格式就交付 | 漏掉 P2 类 bug | 写完立刻 grep |
| P9 | 时间线冲突未核对（如 §1 [00:15:32] 叫"志梁谈谈你"，但说话人3 是 [00:20:07] 才到场） | 说话人误识别遗留 | 时间线必查 |
| P10 | 子目录命名与原文转写文件混淆 | 产物和原文不分 | 子目录固定 `<场次名>_YYYY-MM-DD/` |

## 操作总览

```bash
# 1. 建子目录
mkdir -p "<项目根>/<场次名>_YYYY-MM-DD"
mkdir -p "<项目根>/<场次名>_YYYY-MM-DD/02_主题整理"

# 2. 写 01_清洗稿（分块 Write/Edit，详见 references/02）
# 3. 写 02-09 模块（详见 references/03 + 07）
# 4. 渲染 HTML
python ~/.claude/skills/transcript-to-knowledge-pipeline/scripts/render_pack.py <场次目录>

# 5. 6 次发布前必查（references/05）
# 6. 写 README.md（包含跨场呼应）
```

## 与已有三场知识包的关系

本 skill 已用于以下场次（持续累积）：

| 日期 | 场次 | 子目录 |
|---|---|---|
| 2026-07-17 | 聊聊AI工业应用（首场 5h，4人） | 根目录 |
| 2026-07-21 | 管线转型讨论（1h45，咨询类 2 人） | `管线转型讨论_2026-07-21/` |
| 2026-08-09 | 企业级AI Agent商业化讨论（2h57，4 人：郭磊/Harry/郭志梁/夏老师，**说话人3 = 郭志梁**） | `企业级AI Agent商业化讨论_2026-08-09/` |

跨场呼应在每场的 README、洞察卡片、议题地图里标注（如「本场卡 X = 上场卡 Y」「碎石路→康庄大道 = 土路→高速公路」）。