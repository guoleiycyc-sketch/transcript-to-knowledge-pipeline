---
name: transcript-to-knowledge-pipeline
description: "当用户要把对话录音的转写稿（飞书妙记/腾讯会议/得到大脑/Whisper/手工稿）加工成结构化成果时使用：说话人映射、ASR 错字订正对照表、9 模块知识包、双击即开可全文搜索、金句跳时间戳的 HTML。以下意图一律触发：把转写稿/录音清洗、整理成知识包、可搜索网页或 HTML 文档；新录音到了，按「老规矩/标准管线出全套」「复现上次那条管线」；一批存量旧录音批量清洗；委派模式（轻量模型子代理出清洗稿、主对话做映射与质检）。仅快速回顾一场录音、检索往期录音里的观点原话、把已有的 01 清洗稿改写成会议纪要、出评估分析报告的，不用本 skill。"
display_name: "郭磊的录音挖矿机"
display_name_en: "Guolei's Recording Miner"
description_zh: "对话录音转写稿清洗：说话人映射+逐回合还原，产出 9 模块知识包与可搜索 HTML"
description_en: "Clean dialogue transcripts into a 9-module knowledge pack + searchable HTML"
category: "Productivity"
author: "Guo Lei"
version: 3.1.4
---

# Transcript → Knowledge Pipeline

把一份对话类录音转写稿，严格、零幻觉地整理成结构化知识包 + 可分享 HTML 的工程化管线。

**为什么强调「严格、零幻觉」**：转写稿的知识价值全在核证链——每条金句能跳回时间戳、每个说话人身份可信、每处订正有对照依据。核证链一断，产出就从资产变成幻觉源。本管线的全部纪律都为此服务。

**输入来源**：飞书妙记 / 腾讯会议导出 / 得到大脑 / Whisper / 本地音频转写 / 手工粘贴稿。具体接入路由见 `references/01` 的 Intake 表。

## 管线全貌（5 阶段 + 质量门禁）

```
① Intake + 说话人映射  ←—— 最高风险阶段（含「账号名≠本人」陷阱）
      ↓（开工必问：能否提供参会人名单+背景？见 references/01 Step 0）
      ↓（委派模式：映射后由轻量模型子代理生产 01，见 references/08；主对话保留映射/质检/模块提炼）
      ↓
② 清洗（01_清洗稿）    ←—— 双源字段必写；标签脏数据检测 + 修复两道
      ↓
③ 9 模块产出（00、02–09）
      ↓
④ HTML 组装（render_pack.py）
      ↓
⑤ 跨场呼应 + README
      ↓
╔═══════════════════════════════════════════════════╗
║ 发布前质量门禁（一键）：                            ║
║ bash scripts/pipeline_check.sh <场次目录>           ║
║ 引文核证：python3 scripts/verify_quotes.py <场次目录> ║
╚═══════════════════════════════════════════════════╝
```

每阶段开工前必读对应 references（含该阶段退出自检清单）：

| 阶段 | 参考 |
|---|---|
| ① Intake + 说话人映射 | `references/01-intake-speaker-mapping.md` |
| ② 清洗 + ASR 订正 | `references/02-cleansing-asr.md` |
| ③ 9 模块（格式契约+模板+渲染标记） | `references/03-modules-and-templates.md` |
| ④ HTML 组装 | `references/04-html-rendering.md` |
| ⑤ 质量门禁（发布前必查） | `references/05-quality-gates.md` |
| **多场次知识库**（场次完成后同步） | `references/07-multi-session-kb.md` |
| **子代理模式**（批量清洗 / 单场委派） | `references/08-subagent-modes.md` |
| 坑库（按阶段归类，持续累积） | `references/06-pitfalls.md` |

## 核心纪律（全阶段通用）

### 1. 金句/证据格式（渲染器的识别契约）

所有金句、原文节选、洞察证据、人物代表发言一律：

```
**【姓名】** [MM:SS] 或 [HH:MM:SS] 原话
```

错一个 `】`，渲染器就识别不了 → 整篇金句丢失交互。写完立刻自检：

```bash
grep -cE '^\*\*【[^】]*\*\* \[' <场次目录>/01_清洗稿.md   # 应 = 0
```

### 2. 说话人映射（最高风险，绝不凭称呼判定）

「被叫什么」≠「是谁」：称呼可能是绰号、旧称、同音误听，转写标签还可能是会议账号名而非本人。**开工第一件事：问用户能否提供参会人名单+每人一句背景**——这一问不可跳过（用户答「没有」也正常，退回指纹法），名单能把映射从盲配变验配（见 `references/01` Step 0）；判定必须走**角色指纹 + 时间线核对 + 用户二次确认**，完整方法论见 `references/01`。

### 3. 大文件分块写

`Write` 一次写超大文件（> 5 万字节或大量特殊字符）会被静默截断。清洗稿一律分块：Write 头部+前几节（带 `<!-- APPEND_MARKER_N -->` 锚点）→ Edit 逐块替换锚点追加 → 末尾清锚（`grep -c APPEND_MARKER` 应 = 0）。

### 4. 忠实原则

- 金句原话一字不改（引文从清洗稿逐字复制，禁凭记忆写）。
- ASR 能确定的订正写进头部对照表；不确定的标 `[ASR/存疑]`，**不臆造**。
- 「诚实标注 > 完美无瑕」：拿不准就标注，宁可留缺口不留假话。

### 5. 跨层引用而非复制

同一内容只在一处存真身（金句真身=01 的回合），其他模块用引用/指针。跨层复制正文会让订正一处漏两处、跨场无法归并。

## 产物结构

每场产出一个独立子目录：

```
<项目根>/
  <场次名>_YYYY-MM-DD/
    ├── 00_执行摘要.md             ← BLUF 四段式；渲染时全四节进①可折叠摘要卡
    ├── 01_清洗稿.md               ← 素材层，不进 HTML，是全部引文的核证源头
    ├── 02_主题整理/               ← 每段一个文件；只做描述性编码
    ├── 03_方法论清单.md           ← 只收"遇 X→执行 Y"型 + 本场新增
    ├── 04_术语表.md               ← 只留外部名词；人名→05 指针、方法论→03 指针
    ├── 05_人物角色卡.md           ← ## 人名 + **背景与立场**： + 约 XX%
    ├── 06_关键数据速查.md
    ├── 07_议题关联地图.md         ← **母题**： + ```ASCII 图（只画本场结构）
    ├── 08_洞察卡片.md             ← 每张 = 核心/证据/启示/反方（反方必填）
    ├── 09_战略诊断与行动清单.md   ← 凡有待办即出；开头=上期行动回顾（文件名固定，渲染器按此检测）
    ├── README.md
    └── <场次名>.html              ← render_pack.py 一键生成（文件名=场次目录名）
  _全局层/                          ← 跨场累积层（目录名按项目习惯，可选，见 references/07）
```

各模块的精确格式契约与可复制模板见 `references/03`。

### 产前分档（写 00 前先定）

三问：①决策密度（改变在途决策？）②外部受众（发团队/客户？）③资产含量（可复用方法/金句多寡？）。据此定档，判错可升不可降：

| 档 | 适用 | 模块 |
|---|---|---|
| 极轻 | 纯私人社交/收藏 | 仅 01 |
| 轻 | 信息场/闲聊/破碎转写 | 00+01 |
| 轻加强 | 外部音视频素材 | 01+00+02 单段+03+05+06+08 |
| 专项 | 用户点名只要某模块 | 01+指定模块 |
| 标准 | 参访/训后/产品讨论 | 00+轻量 02+01 |
| 重装 | 咨询/谈判/战略对齐 | 全套 00+01–09+HTML |

## 操作总览

```bash
# 1. 建子目录
mkdir -p "<项目根>/<场次名>_YYYY-MM-DD/02_主题整理"

# 2. 写 01_清洗稿（分块 Write/Edit，体例见 references/02）
# 3. 写 00、02-09 模块（格式契约见 references/03）
# 4. 渲染 HTML（脚本路径相对本 skill 目录）
python3 <skill目录>/scripts/render_pack.py <场次目录>

# 5. 发布前质量门禁（一键，含收尾三问）
bash <skill目录>/scripts/pipeline_check.sh <场次目录>
# 引文核证（有 02/05/08 的场次必跑，硬伤 exit 1）
python3 <skill目录>/scripts/verify_quotes.py <场次目录>

# 6. 写 README.md（含跨场呼应）
# 7. 多场次知识库同步（见 references/07）
# 8. 全局层维护（每场完成后，可选）：
#    - 抽取原子：python3 <skill目录>/scripts/extract_atoms.py <项目根>
#    - 生成视图：python3 <skill目录>/scripts/render_views.py <项目根> --view methods|quotes|share
#      share = 脱敏分享版 HTML（过滤价格/健康级引语，可直接发团队/客户）
```

## 配套工具

| 场景 | 命令 |
|---|---|
| 新项目初始化（config+词表+来源索引三件套） | `python3 <skill目录>/scripts/init_project.py <项目根> [--delegation]` |
| 装机/改动后端到端自检 | `python3 <skill目录>/scripts/selftest.py` |
| 单场脱敏分享版 HTML | `render_pack.py <场次目录> --share [--alias "陈岸→A,林一→B"]` |
| 清洗忠实度审计（原稿 vs 清洗稿机械对比） | `python3 <skill目录>/scripts/diff_report.py <原稿.md> <场次目录>` |
| 看完整示例产物 | `example/示例场_SaaS定价讨论_2026-01-15/`（双击 HTML） |

## 每阶段最容易翻车的事（完整坑库见 `references/06`）

- **映射期**：凭称呼判定身份；一次问完所有映射（只问最不确定的一个，保留追问余地）
- **清洗期**：漏写右 `】`；分段读原文时脑补衔接段；正文回合加粗（炸下游逐字校验）
- **提炼期**：引文凭记忆写（时间戳/措辞失配）；洞察卡反方写成免责声明
- **渲染期**：段文件名/字段头不守契约（渲染器静默提 0，不报错）
- **交付后**：重写 01 不重核下游引文；身份订正不做跨文件波及清理
